
Encoder (now writer) to write audio mxf; and sort out creation of those mxfs
Get rid of DCP job entirely and make encoder write XML (since it has the *Asset objects)
... perhaps generate the CPL hash on the fly
Write hashes of frames after successful write.
Make check of hashes optional; recovery in general

